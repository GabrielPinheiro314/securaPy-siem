import socket

def iniciar_servidor(host='0.0.0.0', port=9999):
    """
    Módulo 4 - Servidor TCP.
    Responsável por receber alertas gerados pelo SIEM e 
    distribuir para clientes conectados (ex: painel de SOC).
    """
    print(f"[servidor_alertas] Stub para servidor TCP em {host}:{port}")
